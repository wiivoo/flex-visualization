---
phase: 09-management-dashboard-proj-40-exec-facing-management-page-wit
plan: 04
type: execute
wave: 3
depends_on: [09-01-PLAN.md, 09-02-PLAN.md, 09-03-PLAN.md]
files_modified:
  - src/app/management/page.tsx
  - src/middleware.ts
autonomous: true
requirements: [MGMT-01, MGMT-08, MGMT-09]
must_haves:
  truths:
    - "GET /management returns 200 for an authenticated session and redirects to /login otherwise"
    - "Page renders four KPI tiles, a YoY bar chart, and an explainer panel when data is present"
    - "Time-period toggle switches KPI values between YTD, Last 12 months, and All time without page reload"
    - "Sum of monthly savings in the KPI tile equals the reconciliation total within 1%"
    - "Settings drawer opens, persists changes, and re-renders KPIs and explainer with the new scenario"
    - "When /data/management-monthly.json is 404 or empty, page renders a graceful empty-state instead of crashing"
  artifacts:
    - path: "src/app/management/page.tsx"
      contains: "'use client'"
      min_lines: 80
    - path: "src/middleware.ts"
      contains: "/management"
  key_links:
    - from: "src/app/management/page.tsx"
      to: "public/data/management-monthly.json"
      via: "fetch(MANAGEMENT_DATA_URL)"
      pattern: "management-monthly\\.json|MANAGEMENT_DATA_URL"
    - from: "src/app/management/page.tsx"
      to: "src/components/management/*"
      via: "component imports"
      pattern: "@/components/management"
    - from: "src/middleware.ts"
      to: "flexmon-session cookie"
      via: "JWT verify in middleware"
      pattern: "flexmon-session"
---

<objective>
Wire everything together: build the `/management` route, guard it with the existing `flexmon-session` password gate, load the precomputed JSON, let the viewer toggle time period, open the settings drawer, and see the KPI tiles, YoY chart, and explainer re-render coherently. Handle missing/empty data gracefully.

Purpose: Deliver the end-user route and prove the phase's main truth — that monthly totals reconcile with `spread × energyPerSession × sessions` within 1%.

Output:
- `src/app/management/page.tsx`
- Updated `src/middleware.ts` (matcher includes `/management`)
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@CLAUDE.md
@.claude/rules/frontend.md
@.claude/rules/security.md
@features/PROJ-40-management-dashboard.md
@src/app/v2/page.tsx
@src/middleware.ts
@src/app/api/auth/route.ts
@src/lib/management-config.ts
@src/lib/management-helpers.ts
@src/components/management/KpiTile.tsx
@src/components/management/YoyBarChart.tsx
@src/components/management/ExplainerPanel.tsx
@src/components/management/SettingsDrawer.tsx

<interfaces>
<!-- Components + helpers this page composes -->

```typescript
// From plan 09-01
import {
  DEFAULT_MANAGEMENT_SCENARIO,
  MANAGEMENT_STORAGE_KEY,
  MANAGEMENT_DATA_URL,
  type ManagementScenario,
  type ManagementDataset,
  type MonthlyAggregate,
  type YoyDatum,
  type ExplainerData,
} from "@/lib/management-config";
import { aggregateMonthly, computeYoy, computeExplainer } from "@/lib/management-helpers";

// From plan 09-02
import { KpiTile } from "@/components/management/KpiTile";
import { YoyBarChart } from "@/components/management/YoyBarChart";

// From plan 09-03
import { ExplainerPanel } from "@/components/management/ExplainerPanel";
import { SettingsDrawer, loadScenarioFromStorage } from "@/components/management/SettingsDrawer";
```

Auth cookie name (per CLAUDE.md): `flexmon-session`. Middleware currently has matcher `[]`; we need to extend it.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Extend middleware to gate /management behind flexmon-session</name>
  <files>src/middleware.ts</files>
  <read_first>
    - src/middleware.ts (current matcher and exports)
    - src/app/api/auth/route.ts (cookie name + jose verify pattern)
    - src/app/login/page.tsx (destination for unauthenticated users)
    - .claude/rules/security.md (auth rules)
  </read_first>
  <action>
    Open `src/middleware.ts`.

    If the matcher is currently `[]` (or the middleware is a no-op), convert it to a real gate:
    1. Import `NextResponse`, `NextRequest` from `next/server`.
    2. Import `jwtVerify` from `jose`.
    3. Implement an async `middleware(req: NextRequest)`:
       - Read `req.cookies.get('flexmon-session')?.value`.
       - If missing -> `NextResponse.redirect(new URL('/login?next=' + encodeURIComponent(req.nextUrl.pathname), req.url))`.
       - Else try `await jwtVerify(token, new TextEncoder().encode(process.env.AUTH_SECRET || process.env.DASHBOARD_SESSION_SECRET || ''))`; on failure -> same redirect to `/login`.
       - On success -> `NextResponse.next()`.
    4. Export `config = { matcher: ['/management/:path*'] }`. Leave `/v2` as-is if it's already covered by page-level checks (do not regress existing behaviour).

    If middleware already enforces auth for `/v2` by matcher, simply ADD `/management/:path*` to the matcher array; keep the existing verification logic.

    Do NOT remove existing matchers or change existing verification. Additive only.
  </action>
  <verify>
    <automated>grep -q "/management" src/middleware.ts &amp;&amp; grep -q "flexmon-session" src/middleware.ts &amp;&amp; npx tsc --noEmit -p tsconfig.json</automated>
  </verify>
  <acceptance_criteria>
    - `grep -q "'/management" src/middleware.ts || grep -q '"/management' src/middleware.ts` succeeds.
    - `grep -q "flexmon-session" src/middleware.ts` succeeds.
    - `grep -q "jwtVerify" src/middleware.ts` succeeds.
    - `grep -q "NextResponse.redirect" src/middleware.ts` succeeds.
    - `grep -q "/login" src/middleware.ts` succeeds.
    - `grep -q "export const config" src/middleware.ts` succeeds.
    - `npx tsc --noEmit` passes.
  </acceptance_criteria>
  <done>Middleware redirects unauthenticated requests to `/management` to `/login` and returns `next()` for valid sessions.</done>
</task>

<task type="auto">
  <name>Task 2: Build /management page with data load, KPIs, YoY, explainer, settings drawer</name>
  <files>src/app/management/page.tsx</files>
  <read_first>
    - src/app/v2/page.tsx (existing client page pattern — Suspense, URL sync, layout)
    - src/lib/management-config.ts (types + URL constant)
    - src/lib/management-helpers.ts (aggregation fns)
    - src/components/management/KpiTile.tsx
    - src/components/management/YoyBarChart.tsx
    - src/components/management/ExplainerPanel.tsx
    - src/components/management/SettingsDrawer.tsx
    - features/PROJ-40-management-dashboard.md (page layout — KPI tile labels, toggle copy, empty-state copy)
  </read_first>
  <action>
    Create `src/app/management/page.tsx`. First line `'use client';`.

    Structure:

    1. Top of file:
       ```tsx
       'use client';
       import { Suspense, useEffect, useMemo, useState } from 'react';
       import { Button } from '@/components/ui/button';
       import { KpiTile } from '@/components/management/KpiTile';
       import { YoyBarChart } from '@/components/management/YoyBarChart';
       import { ExplainerPanel } from '@/components/management/ExplainerPanel';
       import { SettingsDrawer, loadScenarioFromStorage } from '@/components/management/SettingsDrawer';
       import {
         DEFAULT_MANAGEMENT_SCENARIO,
         MANAGEMENT_DATA_URL,
         type ManagementDataset,
         type ManagementScenario,
         type MonthlyAggregate,
         type YoyDatum,
       } from '@/lib/management-config';
       import { computeYoy } from '@/lib/management-helpers';
       ```

    2. Default export `ManagementPage` wraps the real body in `<Suspense fallback={<PageSkeleton />}>` (same pattern as `src/app/v2/page.tsx`).

    3. Inner `ManagementBody` component:
       - State: `dataset: ManagementDataset | null`, `loading: boolean`, `loadError: string | null`, `scenario: ManagementScenario` (init from `loadScenarioFromStorage()` in a `useEffect` to stay SSR-safe — initial render uses `DEFAULT_MANAGEMENT_SCENARIO`), `period: 'YTD' | 'LAST_12' | 'ALL'` (default `'LAST_12'`), `drawerOpen: boolean`.
       - `useEffect`: fetch `MANAGEMENT_DATA_URL`.
         - On 200 + valid JSON: `setDataset(json)`.
         - On 404 or JSON parse error: `setLoadError('No management data available yet')`.
       - `useMemo` for `filteredMonthly`:
         - Input `dataset.monthly` filtered by `period`.
         - `YTD`: months whose year === current year.
         - `LAST_12`: last 12 months ending with the latest in `monthly`.
         - `ALL`: identity.
       - `useMemo` for `yoy`: `computeYoy(dataset.monthly, yearA, yearB)` where `yearA = latestYear - 1`, `yearB = latestYear`.
       - `useMemo` for `kpis`: derive 4 KPI numbers from `filteredMonthly`:
         a. `Total savings (period)` = sum of `savingsEur`.
         b. `Avg spread` = weighted mean of `avgSpreadCtKwh` by `sessionsInMonth`.
         c. `Sessions covered` = sum of `sessionsInMonth`, rounded.
         d. `Avg day-ahead price` = mean of `avgDayAheadCtKwh`.
         Also derive `deltaPct` for each KPI by comparing to the same-length prior window (or `null` if not enough data).
         Sparkline: last 12 monthly `savingsEur` values (pad with zeros if fewer).

    4. Layout (Tailwind grid):
       - Header row: title `Flex Value Dashboard` + `Period` segmented buttons (`YTD`, `Last 12 months`, `All`) + `Settings` button (opens drawer).
       - KPI row: `<div className="grid grid-cols-1 md:grid-cols-2 lg:grid-cols-4 gap-4">` of 4 `<KpiTile>`.
       - YoY panel: `<YoyBarChart data={yoy} yearALabel={String(yearA)} yearBLabel={String(yearB)} />` inside a Card with title `Year over year`.
       - Explainer: `<ExplainerPanel data={dataset.explainer} scenario={scenario} monthlySavingsEur={latestMonthlySavings} />`.
       - Drawer: `<SettingsDrawer open={drawerOpen} onOpenChange={setDrawerOpen} scenario={scenario} onChange={setScenario} />`.

    5. Time-period toggle: 3 buttons with active styling (`#EA1C0A` border/text for active, gray otherwise). Use `tabular-nums` labels.

    6. Empty-state:
       - While loading -> show `PageSkeleton` (three gray boxes mirroring the KPI grid and the chart cards).
       - If `loadError != null || dataset == null || dataset.monthly.length === 0`: render a centered Card with `text-sm text-muted-foreground` message: `"No management data available yet. Run scripts/precompute-management-monthly.mjs to generate it, or wait for the next scheduled workflow."`
       - Never crash on missing fields. Use `?.` and fallbacks.

    7. Scenario note: when `JSON.stringify(scenario) !== JSON.stringify(DEFAULT_MANAGEMENT_SCENARIO)`, render a small banner above the KPI row: `"Custom scenario active — numbers reflect your overrides, not the dashboard defaults."` in `text-[11px] text-amber-700 bg-amber-50 border border-amber-200 rounded px-2 py-1`.

    Notes:
    - Do NOT recompute monthly aggregates from raw QH prices at runtime — use the precomputed dataset. (Rationale: ships on Vercel free tier; raw QH JSON is large.) Scenario overrides should still update KPI calculations by re-aggregating from the precomputed `monthly` entries using the current scenario's energy-per-session factor: for each MonthlyAggregate, recompute `savingsEur = avgSpreadCtKwh/100 * energyPerSession(scenario) * (scenario.sessionsPerWeek * daysInMonth / 7)`.
    - Use `new Intl.NumberFormat('de-DE', ...)` everywhere for consistency.
    - Brand red `#EA1C0A` for the active period pill and sparkline.
  </action>
  <verify>
    <automated>npx tsc --noEmit -p tsconfig.json &amp;&amp; npm run build</automated>
  </verify>
  <acceptance_criteria>
    - File exists at `src/app/management/page.tsx`.
    - First line is `'use client';` or `"use client";`.
    - `grep -q "export default" src/app/management/page.tsx` succeeds.
    - `grep -q "Suspense" src/app/management/page.tsx` succeeds.
    - `grep -q "KpiTile" src/app/management/page.tsx` succeeds.
    - `grep -q "YoyBarChart" src/app/management/page.tsx` succeeds.
    - `grep -q "ExplainerPanel" src/app/management/page.tsx` succeeds.
    - `grep -q "SettingsDrawer" src/app/management/page.tsx` succeeds.
    - `grep -q "loadScenarioFromStorage" src/app/management/page.tsx` succeeds.
    - `grep -q "MANAGEMENT_DATA_URL" src/app/management/page.tsx` succeeds.
    - `grep -qE "'YTD'|\"YTD\"" src/app/management/page.tsx` succeeds.
    - `grep -qE "'LAST_12'|\"LAST_12\"" src/app/management/page.tsx` succeeds.
    - `grep -q "No management data available yet" src/app/management/page.tsx` succeeds.
    - `grep -q "Custom scenario active" src/app/management/page.tsx` succeeds.
    - `grep -q "#EA1C0A" src/app/management/page.tsx` succeeds.
    - File is at least 80 non-empty lines: `awk 'NF' src/app/management/page.tsx | wc -l` >= 80.
    - `npx tsc --noEmit` passes.
    - `npm run build` succeeds.
  </acceptance_criteria>
  <done>`/management` renders four KPI tiles, YoY chart, explainer, and settings drawer; toggles between YTD/Last 12/All; empty-state handled; production build passes.</done>
</task>

<task type="auto">
  <name>Task 3: End-to-end reconciliation and empty-state smoke check</name>
  <files>src/app/management/page.tsx</files>
  <read_first>
    - src/app/management/page.tsx (from Task 2)
    - public/data/management-monthly.json (from 09-01)
    - features/PROJ-40-management-dashboard.md (reconciliation truth)
  </read_first>
  <action>
    No new files. Run a local smoke test:

    1. Start dev server: `npm run dev` (background).
    2. Without a `flexmon-session` cookie, `curl -sI http://localhost:3000/management` MUST return `307` or `308` redirect to `/login?next=/management`.
    3. Obtain a session via `curl -s -X POST -H 'Content-Type: application/json' -d '{"password":"$DASHBOARD_PASSWORD"}' -c /tmp/jar.txt http://localhost:3000/api/auth`.
    4. `curl -sI -b /tmp/jar.txt http://localhost:3000/management` MUST return `200`.
    5. `curl -s -b /tmp/jar.txt http://localhost:3000/management | grep -qE "Flex Value Dashboard|Year over year"` MUST succeed.
    6. Temporarily rename `public/data/management-monthly.json` to `.bak`, reload the page, confirm the empty-state copy renders (`"No management data available yet"`). Restore the file.

    Capture command output. If any step fails, fix the underlying component (middleware, page, fetch) before closing.

    This task exists to prove the end-to-end wiring before we claim the phase done. No production code changes unless a failure is found.
  </action>
  <verify>
    <automated>npm run build</automated>
  </verify>
  <acceptance_criteria>
    - `npm run build` passes.
    - Unauthenticated request to `/management` redirects to `/login` (status in {307, 308}).
    - Authenticated request to `/management` returns 200 and HTML containing "Flex Value Dashboard".
    - With `public/data/management-monthly.json` absent, page renders the empty-state copy instead of crashing.
  </acceptance_criteria>
  <done>Smoke test passes; dashboard reconciles and gracefully degrades without data.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries
| Boundary | Description |
|----------|-------------|
| browser -> `/management` | Password-gated; unauthenticated requests redirect to `/login` |
| page -> localStorage | Client-only scenario persistence |

## STRIDE Threat Register
| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-09-P-01 | Information Disclosure | `/management` page | mitigate | Middleware enforces `flexmon-session` JWT; page-level fallback also redirects if state indicates no session (defence-in-depth). |
| T-09-P-02 | Tampering | scenario overrides in URL/localStorage | accept | Client-side only; no effect on server-served JSON. |
| T-09-P-03 | Spoofing | auth token forgery | mitigate | `jose.jwtVerify` with `AUTH_SECRET`/`DASHBOARD_SESSION_SECRET`; reuses existing proven mechanism from `/v2`. |
| T-09-P-04 | Denial of Service | missing data file | mitigate | Empty-state fallback; page does not throw on `fetch` failure. |
</threat_model>

<verification>
- Middleware redirect works for unauthenticated users.
- Authenticated `/management` returns 200 and renders all four sections.
- Reconciliation equation in ExplainerPanel matches the sum shown in the "Total savings" KPI within 1%.
- Empty-state renders when `public/data/management-monthly.json` is absent.
- `npm run build` and `npm run lint` pass.
</verification>

<success_criteria>
Phase 09 is done when Plan 09-04 tasks all pass and the five phase-level `must_haves.truths` can be demonstrated live.
</success_criteria>

<output>
After completion, create `.planning/phases/09-management-dashboard-proj-40-exec-facing-management-page-wit/09-04-SUMMARY.md` using the summary template.
</output>
