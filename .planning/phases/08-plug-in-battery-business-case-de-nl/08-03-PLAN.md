---
phase: 08-plug-in-battery-business-case-de-nl
plan: 03
type: execute
wave: 1
depends_on: []
files_modified:
  - src/components/ui/select.tsx
  - package.json
  - package-lock.json
  - components.json
  - features/PROJ-39-plug-in-battery-business-case.md
  - features/INDEX.md
  - .planning/REQUIREMENTS.md
autonomous: true
requirements:
  - BATT-11
tags: [shadcn, feature-spec, requirements]

must_haves:
  truths:
    - "The shadcn <Select> primitive exists at src/components/ui/select.tsx and can be imported as `import { Select, SelectTrigger, SelectValue, SelectContent, SelectItem } from '@/components/ui/select'`."
    - "features/PROJ-39-plug-in-battery-business-case.md exists with header fields (Status: In Progress, Owner: Lars, Phase: 8) and scope summary."
    - "features/INDEX.md has a new row for PROJ-39 and 'Next Available ID' is updated to PROJ-40."
    - ".planning/REQUIREMENTS.md lists BATT-01..BATT-11 under a new 'Battery Business Case' section, each tagged to Phase 8."
    - "`npm run build` succeeds with the new select primitive imported (no broken imports)."
  artifacts:
    - path: "src/components/ui/select.tsx"
      provides: "shadcn Select primitive (Radix-wrapped), used by RegulationPanel + BatteryVariantPicker tariff dropdown"
      exports:
        - "Select"
        - "SelectContent"
        - "SelectGroup"
        - "SelectItem"
        - "SelectLabel"
        - "SelectSeparator"
        - "SelectTrigger"
        - "SelectValue"
      min_lines: 80
    - path: "features/PROJ-39-plug-in-battery-business-case.md"
      provides: "Feature spec header and scope for /battery page work"
      contains: "PROJ-39"
      min_lines: 40
    - path: ".planning/REQUIREMENTS.md"
      provides: "BATT-01..BATT-11 requirement entries"
      contains: "BATT-01"
  key_links:
    - from: "features/INDEX.md"
      to: "features/PROJ-39-plug-in-battery-business-case.md"
      via: "Markdown link in Active Features table"
      pattern: "PROJ-39"
    - from: "src/components/ui/select.tsx"
      to: "@radix-ui/react-select"
      via: "import statements"
      pattern: "@radix-ui/react-select"
---

<objective>
Handle the three administrative/setup items that are independent of the optimizer and component work:

1. Install the shadcn `select` primitive (needed by RegulationPanel NL export-compensation select and the BatteryVariantPicker tariff dropdown).
2. Create the feature spec at features/PROJ-39-plug-in-battery-business-case.md and update features/INDEX.md.
3. Append BATT-01..BATT-11 requirement entries to .planning/REQUIREMENTS.md with traceability to Phase 8.

Purpose: Runs parallel to 08-01 (profiles) and 08-02 (types). Removes setup friction from Wave 3 so page+component work can assume Select is available and PROJ-39 is the tracking ID.

Output:
- src/components/ui/select.tsx (shadcn-generated)
- features/PROJ-39-plug-in-battery-business-case.md (new)
- features/INDEX.md (updated)
- .planning/REQUIREMENTS.md (updated)
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/08-plug-in-battery-business-case-de-nl/08-CONTEXT.md
@.planning/phases/08-plug-in-battery-business-case-de-nl/08-UI-SPEC.md
@features/INDEX.md
@features/PROJ-29-v2g-dual-value-streams.md
@.planning/REQUIREMENTS.md
@CLAUDE.md

<interfaces>
<!-- Expected select primitive export surface (shadcn standard): -->
```typescript
// src/components/ui/select.tsx — shadcn generated, imports @radix-ui/react-select
export {
  Select,
  SelectContent,
  SelectGroup,
  SelectItem,
  SelectLabel,
  SelectSeparator,
  SelectTrigger,
  SelectValue,
}
```

<!-- Feature spec header contract (mirrors PROJ-29): -->
```markdown
# PROJ-39 — Plug-in Battery Business Case (DE/NL)

**Status:** In Progress
**Owner:** Lars
**Phase:** 8
**Created:** 2026-04-17
**Scope:** New /battery sub-page modelling home-battery ROI ...
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Install shadcn select primitive</name>
  <files>src/components/ui/select.tsx, package.json, package-lock.json, components.json</files>
  <read_first>
    - components.json (confirm style/preset/cssVariables flags already in place)
    - src/components/ui/tooltip.tsx (reference for existing shadcn pattern: forwardRef + cn utility)
  </read_first>
  <action>
Run the shadcn CLI to add the select primitive:

```bash
npx shadcn@latest add select --yes
```

This will:
- Create src/components/ui/select.tsx (Radix-wrapped Select with shadcn defaults)
- Add @radix-ui/react-select to package.json dependencies
- Update package-lock.json

Expected output file (shadcn standard). If the CLI writes a different export shape, DO NOT edit the shadcn output — the shadcn CLI is canonical.

If the CLI prompts interactively (should not with --yes), abort and fall back to manual install:
1. `npm install @radix-ui/react-select --save`
2. Copy the canonical select.tsx from https://ui.shadcn.com/r/styles/new-york-v4/select.json (or the existing dialog.tsx pattern as a local reference).

Verify Radix select version is compatible with existing Radix primitives in package.json (alert-dialog ^1.1.15, dialog ^1.1.15) — shadcn pins versions consistently.
  </action>
  <verify>
    <automated>ls -la src/components/ui/select.tsx &amp;&amp; grep -c '@radix-ui/react-select' package.json &amp;&amp; npx tsc --noEmit -p . 2>&amp;1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - `test -f src/components/ui/select.tsx` succeeds.
    - `grep -c '@radix-ui/react-select' package.json` returns exactly 1.
    - `grep -c 'export' src/components/ui/select.tsx` returns at least 8 (Select, SelectContent, SelectGroup, SelectItem, SelectLabel, SelectSeparator, SelectTrigger, SelectValue).
    - `grep -c 'SelectTrigger' src/components/ui/select.tsx` returns at least 2 (definition + export).
    - `grep -c 'SelectContent' src/components/ui/select.tsx` returns at least 2.
    - `grep -c 'SelectItem' src/components/ui/select.tsx` returns at least 2.
    - `grep -c 'SelectValue' src/components/ui/select.tsx` returns at least 2.
    - `npx tsc --noEmit -p .` returns exit code 0.
    - `npm run build 2>&1 | tail -5` shows no errors related to select imports.
  </acceptance_criteria>
  <done>src/components/ui/select.tsx exists, @radix-ui/react-select is in package.json, TypeScript compiles cleanly, build succeeds.</done>
</task>

<task type="auto">
  <name>Task 2: Create features/PROJ-39 spec and update features/INDEX.md</name>
  <files>features/PROJ-39-plug-in-battery-business-case.md, features/INDEX.md</files>
  <read_first>
    - features/PROJ-29-v2g-dual-value-streams.md (closest In Progress feature; mirror header fields)
    - features/INDEX.md (find Active Features table and Next Available ID line)
    - .planning/phases/08-plug-in-battery-business-case-de-nl/08-CONTEXT.md §Phase Boundary (for scope summary)
  </read_first>
  <action>
Create features/PROJ-39-plug-in-battery-business-case.md with this content (adjust header fields to match project convention from PROJ-29):

```markdown
# PROJ-39 — Plug-in Battery Business Case (DE/NL)

**Status:** In Progress
**Owner:** Lars
**Phase:** 8
**Created:** 2026-04-17

## Scope

New /battery sub-page that models the business case for plug-in home batteries in Germany (current 2026 regime) and the Netherlands (post-2027 regime). Covers three product variants with a split layout: consumer ROI calculator on top, management/investor view below.

## Product Variants

1. **Marstek Venus B** — 2 kWh Schuko plug-in, no PV, 19% VAT. Pure dynamic-tariff arbitrage.
2. **Anker SOLIX Solarbank 2 E1600 Pro** — 800 Wp PV + 1.52 kWh battery, 0% VAT (PV bundle). Arbitrage + PV self-consumption.
3. **Marstek Venus E 3.0** — 5.12 kWh wall-mount, electrician-required commissioning, 19% VAT. Larger-capacity arbitrage.

## Value Streams

- Dynamic tariff arbitrage (SMARD DE, ENTSO-E NL)
- Balcony PV self-consumption (PVGIS south-facing 800W array)

## Key Regulation Modeled

- **DE:** 800W feed-in cap (with 2000W sensitivity toggle). VDE-AR-N 4105:2026-03 prohibits battery-to-grid export → optimizer enforces self-consumption only. 19% VAT on standalone, 0% on PV bundles.
- **NL:** Post-2027 regime only (salderingsregeling ended 2027-01-01). Minimum 50% export compensation through 2030. Configurable terugleverkosten.

## Out of Scope

- V2G / EV-as-battery (Phase 1 / PROJ-29)
- C&I / fleet-scale batteries
- Backup / resilience value stream
- NL current-regime (pre-2027) modeling
- Grid export arbitrage in DE (legally prohibited)

## Files

- `src/app/battery/page.tsx` — main page
- `src/components/battery/*` — BatteryVariantPicker, BatteryDayChart, BatteryRoiCard, RegulationPanel, ManagementView
- `src/lib/battery-config.ts` — types, variants, defaults
- `src/lib/battery-optimizer.ts` — greedy three-pass optimizer
- `public/data/bdew-h0-profile.json`, `nedu-e1a-normalized.json`, `pvgis-de-south-800w.json`, `pvgis-nl-south-800w.json`
- `scripts/precompute-battery-profiles.mjs`

## Requirements

BATT-01..BATT-11 — see `.planning/REQUIREMENTS.md`.

## Related

- Phase 8 plans: `.planning/phases/08-plug-in-battery-business-case-de-nl/08-*-PLAN.md`
- Research: `.planning/phases/08-plug-in-battery-business-case-de-nl/08-RESEARCH.md`
- UI contract: `.planning/phases/08-plug-in-battery-business-case-de-nl/08-UI-SPEC.md`
```

Then update features/INDEX.md:
1. Insert a new row in the "Active Features (Deployed)" table immediately after PROJ-37:
   ```
   | PROJ-39 | Plug-in Battery Business Case (DE/NL) | In Progress | [spec](PROJ-39-plug-in-battery-business-case.md) | `app/battery/`, `components/battery/`, `lib/battery-*.ts` |
   ```
2. Change the "Next Available ID" line from `PROJ-38` to `PROJ-40`. (PROJ-38 is reserved by the recent insights/quick-tasks work per git log; skip it.)

DO NOT modify any other rows in the table. DO NOT reorder archived features.
  </action>
  <verify>
    <automated>test -f features/PROJ-39-plug-in-battery-business-case.md &amp;&amp; grep -c 'PROJ-39' features/INDEX.md &amp;&amp; grep -c 'Next Available ID: PROJ-40' features/INDEX.md</automated>
  </verify>
  <acceptance_criteria>
    - `test -f features/PROJ-39-plug-in-battery-business-case.md` succeeds.
    - `grep -c "^# PROJ-39" features/PROJ-39-plug-in-battery-business-case.md` returns 1.
    - `grep -c "Status:\*\* In Progress" features/PROJ-39-plug-in-battery-business-case.md` returns 1.
    - `grep -c "Phase:\*\* 8" features/PROJ-39-plug-in-battery-business-case.md` returns 1.
    - `grep -c "Marstek Venus B" features/PROJ-39-plug-in-battery-business-case.md` returns 1.
    - `grep -c "Anker SOLIX Solarbank 2 E1600 Pro" features/PROJ-39-plug-in-battery-business-case.md` returns 1.
    - `grep -c "Marstek Venus E 3.0" features/PROJ-39-plug-in-battery-business-case.md` returns 1.
    - `grep -c "BATT-01" features/PROJ-39-plug-in-battery-business-case.md` returns 1.
    - `grep -c "PROJ-39" features/INDEX.md` returns at least 1 (active features row).
    - `grep -c "Next Available ID: PROJ-40" features/INDEX.md` returns 1.
    - `grep -c "Next Available ID: PROJ-38" features/INDEX.md` returns 0 (old line replaced).
    - `wc -l features/PROJ-39-plug-in-battery-business-case.md` returns ≥ 40.
  </acceptance_criteria>
  <done>Feature spec created with correct header and scope; INDEX.md has new row and updated next-ID line.</done>
</task>

<task type="auto">
  <name>Task 3: Append BATT-01..BATT-11 to .planning/REQUIREMENTS.md</name>
  <files>.planning/REQUIREMENTS.md</files>
  <read_first>
    - .planning/REQUIREMENTS.md (find the "Active Requirements" section layout; note how V2G-01, INTL-01 etc. are formatted)
  </read_first>
  <action>
Append a new subsection under "Active Requirements (In Progress)" in .planning/REQUIREMENTS.md. Insert it after the Intraday subsection (between "INTRA-05" and the "v2 Requirements (Deferred)" header). Do not modify any existing rows.

New subsection:

```markdown
### Battery Business Case (Phase 8)
- [ ] **BATT-01**: Static profile JSON assets (BDEW H0 DE, NEDU E1a NL, PVGIS DE, PVGIS NL) — Phase 8
- [ ] **BATT-02**: Battery config types and variants (Marstek Venus B, Anker SOLIX Solarbank 2 E1600 Pro, Marstek Venus E 3.0) — Phase 8
- [ ] **BATT-03**: Battery day optimizer with DE grid-export prohibition enforced (`gridExportKwh = 0`) — Phase 8
- [ ] **BATT-04**: Battery annual roll-up (`runBatteryYear`) producing monthly + annual results — Phase 8
- [ ] **BATT-05**: `/battery` page route with URL↔state sync (variant, country, tariff, date, feed-in cap) — Phase 8
- [ ] **BATT-06**: BatteryVariantPicker component with three-variant selection + inline country/tariff/load controls — Phase 8
- [ ] **BATT-07**: BatteryDayChart (ComposedChart: price, load, PV, charge/discharge, SoC) — Phase 8
- [ ] **BATT-08**: BatteryRoiCard with annual savings, simple payback, break-even year, 10yr NPV, 12-month breakdown — Phase 8
- [ ] **BATT-09**: RegulationPanel (DE 800W/2000W toggle, NL terugleverkosten + export compensation) — Phase 8
- [ ] **BATT-10**: ManagementView (DE vs NL unit-economics table + revenue-stream breakdown + market framing) — Phase 8
- [ ] **BATT-11**: Feature spec PROJ-39 + INDEX.md update — Phase 8
```

Also update the Traceability table at the bottom of the file by appending:

```markdown
| BATT-01..11 | Phase 8 | In Progress |
```

Do not change existing Traceability rows.
  </action>
  <verify>
    <automated>grep -c 'BATT-01' .planning/REQUIREMENTS.md &amp;&amp; grep -c 'BATT-11' .planning/REQUIREMENTS.md &amp;&amp; grep -c 'Battery Business Case (Phase 8)' .planning/REQUIREMENTS.md</automated>
  </verify>
  <acceptance_criteria>
    - `grep -cE "\*\*BATT-0[1-9]\*\*" .planning/REQUIREMENTS.md` returns at least 9.
    - `grep -cE "\*\*BATT-1[01]\*\*" .planning/REQUIREMENTS.md` returns at least 2.
    - `grep -c "Battery Business Case (Phase 8)" .planning/REQUIREMENTS.md` returns 1.
    - `grep -c "gridExportKwh = 0" .planning/REQUIREMENTS.md` returns 1.
    - `grep -c "BATT-01..11 | Phase 8 | In Progress" .planning/REQUIREMENTS.md` returns 1.
    - All existing requirement rows are preserved: `grep -c "V2G-01" .planning/REQUIREMENTS.md` is ≥ 2 (unchanged), `grep -c "DATA-01" .planning/REQUIREMENTS.md` is ≥ 2 (unchanged).
  </acceptance_criteria>
  <done>.planning/REQUIREMENTS.md has the new Battery Business Case subsection with BATT-01..BATT-11 and the Traceability row appended. No existing rows modified.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| npm registry → local node_modules | shadcn CLI pulls Radix packages from npm. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-08-03-01 | Tampering | npm package supply chain | accept | Radix + shadcn are widely used with high community scrutiny; lockfile pins exact versions. Existing project convention. |
| T-08-03-02 | Information disclosure | Feature spec content | accept | Public project documentation; no secrets. |
| T-08-03-03 | Denial of service | shadcn CLI interactive prompt | mitigate | Use `--yes` flag to suppress interactive prompts. |
</threat_model>

<verification>
- `test -f src/components/ui/select.tsx` succeeds.
- `npx tsc --noEmit -p .` returns 0.
- `npm run build` succeeds.
- `test -f features/PROJ-39-plug-in-battery-business-case.md` succeeds.
- `grep -c "PROJ-39" features/INDEX.md` returns ≥ 1.
- `grep -c "BATT-01" .planning/REQUIREMENTS.md` returns ≥ 1.
</verification>

<success_criteria>
1. shadcn Select primitive installed and importable via `@/components/ui/select`.
2. features/PROJ-39 spec exists with required header fields and scope.
3. features/INDEX.md updated with PROJ-39 row and next-ID bumped to PROJ-40.
4. .planning/REQUIREMENTS.md contains BATT-01..BATT-11 in new Battery Business Case section + Traceability row.
5. TypeScript compiles and `npm run build` succeeds.
</success_criteria>

<output>
After completion, create `.planning/phases/08-plug-in-battery-business-case-de-nl/08-03-SUMMARY.md` documenting:
- @radix-ui/react-select version installed
- Number of exports in src/components/ui/select.tsx
- Confirmation PROJ-39 spec + INDEX + REQUIREMENTS updates all landed
</output>
